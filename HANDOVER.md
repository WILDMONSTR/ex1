# 인수인계 문서 — 취미 포트폴리오 웹사이트

## 1. 목표

내 취미 포트폴리오 웹사이트를 완성시키는 것.

## 2. 현재 상태

- 단일 페이지 구조로, 한 화면에 소개·활동·근거가 모두 보이도록 통합됨
- 상단: 공개할 것(취미, 취향, 근황) / 공개하지 않을 것(민감 정보, 불호, 소속 기관)
- 중간: 취향 카드 3종 (빵·디저트, 달리는 운동, 게임)
- 하단: 상황·행동·결과(SAR) 카드 + 근거 배지 + 사진 근거
- 사진 근거에는 사용자가 직접 첨부한 5장 사용 중
  - 빵·디저트 2장, 달리는 운동 1장, 게임 2장
- 1366×768, 1920×1080 해상도에서 소개·활동·근거가 한눈에 보이도록 압축 레이아웃 적용

## 3. 해결할 문제

### 사진 비율 및 배치 수정 (현재 진행 중인 요청)

- 사진 근거 상단 영역(빵·디저트 2장, 달리기 1장)의 코드 소스는 건드리지 말 것
- 사진이 원본 비율로 잘리지 않고 전체가 보이도록 `object-contain` 적용 필요
- 화면에 빈 공간이 없도록 비율과 배치 조정 필요

### 접근성 및 상호작용 (남은 문제)

- 모든 항목이 **Tab 키로 순차 이동** 가능해야 함
  - 현재 취향 카드, SAR 카드 등은 `<div>`로 되어 있어 포커스 불가
  - 상호작용 가능한 요소는 `<button>` 또는 `tabindex="0"` + `role` 속성 적용 검토
- 모든 항목이 **마우스 커서에 반응**해야 함
  - `cursor: pointer` 적용, hover 시 시각적 피드백(그림자, 색상 변화 등) 확인
  - 키보드 포커스 시에도 동등한 시각적 피드백 필요 (`:focus-visible` 스타일)

## 4. 주의사항

- **사진 근거 상단 코드 소스 수정 금지** (빵·디저트, 달리기 영역)
- 첨부된 5장의 사진 파일은 `public/images/hobby/` 폴더에 있음
- Supabase는 사용하지 않음 (정적 데이터만 사용하는 프로젝트)

## 5. 기술 스택

- React 18 + TypeScript + Vite
- Tailwind CSS (스타일링)
- lucide-react (아이콘)
- 빌드: `npm run build`, 타입체크: `npm run typecheck`

## 6. 파일 구조

```
src/App.tsx          — 메인 컴포넌트 (모든 UI 포함)
public/images/hobby/ — 첨부된 사진 5장
```
